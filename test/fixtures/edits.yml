welcome_section_creation:
  action: creation
  leaf: welcome_section
  leafable: welcome (Section)

welcome_page_creation:
  action: creation
  leaf: welcome_page
  leafable: welcome (Page)

summary_page_creation:
  action: creation
  leaf: summary_page
  leafable: summary (Page)

reading_picture_creation:
  action: creation
  leaf: reading_picture
  leafable: reading (Picture)
