<% content_for(:title) { "Manage access" } %>

<% content_for :header do %>
  <nav>
    <%= link_to book_path(@book), class: "btn flex-item-justify-start" do %>
      <%= image_tag "arrow-left.svg", aria: { hidden: true }, size: 24 %>
      <span class="for-screen-reader">Go back</span>
    <% end %>

    <div class="breadcrumbs flex-item-justify-start">
      <%= link_to books_path do %>
        <%= image_tag "books.svg", aria: { label: "Books" }, size: 19, class: "colorize--black", alt: "Books" %>
      <% end %> ▸
      <strong><%= @book.title %></strong>
    </div>
  </nav>
<% end %>

<div class="panel borderless center pad fill-none flex flex-column gap">
  <%= render partial: "access", collection: @accesses, as: :user %>
</div>
